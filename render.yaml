services:
  - type: web
    name: whatsapp-impacto-bot
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node servidor_whatsapp_novo.js
    envVars:
      - key: NODE_ENV
        value: production